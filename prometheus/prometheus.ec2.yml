global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'chat-app'
    static_configs:
      - targets: ['chat-app:8000']
        labels:
          service: 'chat'
          environment: 'production'
